/**
 * Optimisations Zone.js avancées pour réduire la taille du bundle
 * et améliorer les performances
 */
// eslint-disable-next-line no-underscore-dangle
(window as any).__Zone_disable_customElements = true;

// Désactiver les patches non essentiels pour réduire la taille
(window as any).__Zone_disable_requestAnimationFrame = true;
(window as any).__Zone_disable_on_property = true;
(window as any).__Zone_disable_timers = true;
(window as any).__Zone_disable_blocking = true;
(window as any).__Zone_disable_eventTarget = true;
(window as any).__Zone_disable_on_property = true;
(window as any).__Zone_disable_canvas = true;
(window as any).__Zone_disable_geolocation = true;
(window as any).__Zone_disable_fileReader = true;
(window as any).__Zone_disable_mediaQuery = true;
(window as any).__Zone_disable_resizeObserver = true;
(window as any).__Zone_disable_intersectionObserver = true;

// Événements non patchés pour de meilleures performances
(window as any).__zone_symbol__UNPATCHED_EVENTS = [
  'scroll', 'mousemove', 'mouseenter', 'mouseleave', 
  'touchstart', 'touchmove', 'touchend', 'pointermove'
];
